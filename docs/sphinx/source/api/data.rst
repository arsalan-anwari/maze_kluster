maze_kluster.data
=================

``TileLogger`` buffers one row per tile visit and appends them to the parquet
dataset on ``flush()``. ``RunLog`` writes a JSONL step trace alongside each run
for use in TUI replay.

.. automodule:: maze_kluster.data.collector
   :members:
   :undoc-members: False
